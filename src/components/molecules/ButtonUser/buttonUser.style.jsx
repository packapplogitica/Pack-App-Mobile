import { createStyles } from "@mantine/core";

const useStyles = createStyles((theme, params) => {
  return {};
});

const ButtonUserStyle = (params) => useStyles(params);

export default ButtonUserStyle;
