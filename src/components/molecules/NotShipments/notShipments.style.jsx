import { createStyles } from "@mantine/core";

const useStyles = createStyles((theme, params) => {
  return {};
});

const NotShipmentsStyle = (params) => useStyles(params);

export default NotShipmentsStyle;
