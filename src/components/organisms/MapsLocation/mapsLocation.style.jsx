import { createStyles } from "@mantine/core";

const MapsLocation = createStyles(() => {
  return {
    maps: {
      height: "400px",
      borderRadius: 10,
    },
  };
});

export default MapsLocation;
